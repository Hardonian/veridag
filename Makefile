# Convenience wrapper around just(1). Requires just on PATH.
.PHONY: setup check vectors formal devnet sim
setup:   ; just setup
check:   ; just check
vectors: ; just vectors
formal:  ; just formal
devnet:  ; just devnet
sim:     ; just sim
